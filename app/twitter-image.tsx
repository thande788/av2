/**
 * Twitter Card Image Generation
 * 
 * Same as OG image but optimized for Twitter cards
 */

import OGImage, { size, contentType, alt } from "./opengraph-image";

export { size, contentType, alt };
export default OGImage;
